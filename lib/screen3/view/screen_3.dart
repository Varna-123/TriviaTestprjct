import 'package:flutter/material.dart';

class Screen_3 extends StatelessWidget {
  const Screen_3({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:const Center(child: Text("TRIVIA APP"))
      
    );
  }
}